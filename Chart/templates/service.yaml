apiVersion: v1
kind: Service
metadata:
  name: {{ include "palworld.fullname" . }}
  labels:
    {{- include "palworld.labels" . | nindent 4 }}
spec:
  type: NodePort
  ports:
    - port: {{ .Values.env.PORT }}
      targetPort: {{ .Values.env.PORT }}
      protocol: TCP
      name: tcp-queryport
    - port: {{ .Values.env.PORT }}
      targetPort: {{ .Values.env.PORT }}
      protocol: UDP
      name: udp-queryport
    - port: {{ .Values.env.PUBLIC_PORT }}
      targetPort: {{ .Values.env.PUBLIC_PORT }}
      protocol: TCP
      name: tcp-gameport
    - port: {{ .Values.env.PUBLIC_PORT }}
      targetPort: {{ .Values.env.PUBLIC_PORT }}
      protocol: UDP
      name: udp-gameport
    {{- include "palworld.selectorLabels" . | nindent 6 }}
